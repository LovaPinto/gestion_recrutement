<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>JobTracker</title>
    <link rel="stylesheet" href="{{ asset('styles/portail.css') }}">
    <link rel="stylesheet" href="{{ asset('styles/emplois.css') }}">
    <link rel="stylesheet" href="{{ asset('styles/head.css') }}">
</head>
<body>
    <header>
        <div>
          <div class="logo">Nextwork</div>
          <div class="slogan">Ensemble, changeons le quotidien des Malagasy</div>
        </div>
        <div class="actions">
          <div class="bell">🔔</div>
          <button class="login-btn">Se connecter</button>
        </div>
      </header>

      <section class="search-section">
        <div class="search-bar">
          <input type="text" placeholder="Search Jobs and keywords" />
          <select>
            <option>Entreprise</option>
            <option>Nexthope</option>
            <option>Autres</option>
          </select>
          <select>
            <option>Département</option>
            <option>IT</option>
            <option>RH</option>
          </select>
          <select>
            <option>Type de contrat</option>
            <option>CDI</option>
            <option>CDD</option>
            <option>Stage</option>
            <option>Freelance</option>
          </select>
        </div>

        <div class="section-title">Postes disponibles</div>
        <div class="job-count">Plus de 3 postes disponibles</div>

        <div class="section-title">Derniers emplois</div>
        <div class="job-list">
          <div class="job-item">
            <div class="job-info">
              <div>Assistant admin et paie</div>
              <div class="contract">CDD</div>
            </div>
            <div>Nexthope</div>
          </div>
          <div class="job-item">
            <div class="job-info">
              <div>Ingénieur Réseau, Système, Sécurité</div>
              <div class="contract">CDD</div>
            </div>
            <div>Nexthope</div>
          </div>
          <div class="job-item">
            <div class="job-info">
              <div>Assistant admin et paie</div>
              <div class="contract">CDI</div>
            </div>
            <div>Nexthope</div>
          </div>
        </div>
      </section>
    <div class="container">
        <section class="section">
          <div class="section-header">
            <h2 class="title">Derniers emplois</h2>
          </div>

          <nav class="filters" aria-label="Filtrer par type de contrat">
            <button class="filter active" type="button">Toutes les offres</button>
            <button class="filter" type="button">CDI</button>
            <button class="filter" type="button">CDD</button>
            <button class="filter" type="button">Stage</button>
            <button class="filter" type="button">Freelance</button>
          </nav>

          <table class="table" role="table">
            <thead>
              <tr>
                <th>Position</th>
                <th>Entreprise</th>
                <th>Contract</th>
              </tr>
            </thead>
            <tbody>
              {% if datas is not empty %}
              {% for job in datas %}
                  <tr>
                      <td class="position" data-label="Position">{{ job.description }}</td>
                      <td class="company" data-label="Entreprise">{{ job.user.department.company.companyName }}</td>
                      <td data-label="Contract"><span class="contract cdd">{{ job.offerType }}</span></td>
                  </tr>
              {% endfor %}
          {% else %}
              <p>Aucune offre disponible</p>
          {% endif %}
            </tbody>
          </table>

          <div class="actions">
            <a class="btn" href="#" aria-label="Voir plus d’emplois">More Jobs &gt;</a>
          </div>
        </section>
      </div>
</body>
</html>
