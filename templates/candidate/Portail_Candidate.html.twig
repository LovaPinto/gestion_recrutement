<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<title>JobTracker</title>

		<link rel="stylesheet" href="{{ asset('styles/portail.css') }}">
		<link rel="stylesheet" href="{{ asset('styles/emplois.css') }}">
		<link rel="stylesheet" href="{{ asset('styles/galerie.css') }}">
	</head>

	<body>

		<!-- Header -->
		<header class="header">
			<div class="logo">
				<span class="orange">Next</span>Work
			</div>
			<div class="login">
				<a href="/login" class="btn-login">Se connecter</a>
			</div>
		</header>

		<!-- Hero Section -->
		<section class="hero">
			<div class="hero-content">
				<h1>Booster votre carrière à Madagascar.</h1>
			</div>

			<!-- Formulaire de recherche -->
			<div class="hero-form">
				<form method="get" action="{{ path('app_job_portail_default') }}">
					<div class="search-form">
						<div class="input-wrapper modern-search">
							<input type="search" name="keyword" id="job-search" placeholder="Rechercher un emploi..." value="{{ keyword }}" onkeyup="this.form.submit()">
							<span class="search-icon">
								<svg width="22" height="22" viewbox="0 0 24 24" fill="none">
									<path d="M15 15L21 21" stroke="#777" stroke-width="2" stroke-linecap="round"/>
									<circle cx="10" cy="10" r="6" stroke="#777" stroke-width="2"/>
								</svg>
							</span>
						</div>
					</div>

					<div class="filters-row">
						<div class="filter-box">
							<label for="company">Entreprise :</label>
							<select name="company" id="company" onchange="this.form.submit()">
								<option value="">Sélectionner</option>
								{% for company in companyNames %}
									<option value="{{ company.companyName }}" {% if company.companyName == selectedCompany %} selected {% endif %}>
										{{ company.companyName }}
									</option>
								{% endfor %}
							</select>

						</div>

						<div class="filter-box">
							<label for="filter-department">Département :</label>
							<select name="department" id="filter-department" onchange="this.form.submit()">
								<option value="">Sélectionner</option>
								{% for department in departmentNames %}
									<option value="{{ department.departmentName }}" {% if department.departmentName == selectedDepartment %} selected {% endif %}>
										{{ department.departmentName }}
									</option>
								{% endfor %}
							</select>
						</div>


						<div class="filter-box">
							<label for="filter-position">Contrat :</label>
							<select name="offerType" id="filter-position" onchange="this.form.submit()">
								<option value="">Sélectionner</option>
								{% for offer in offerTypes %}
									<option value="{{ offer.offerType }}" {% if offer.offerType == selectedOfferType %} selected {% endif %}>{{ offer.offerType }}</option>
								{% endfor %}
							</select>
						</div>
					</div>
				</form>
			</div>
		</section>

		<!-- Tableau des offres de job -->
		<section class="main-content">
			<table class="table">
				<thead>
					<tr>
						<th>Titre</th>
						<th>Type d'offre</th>
						<th>Entreprise</th>
						<th>Département</th>
					</tr>
				</thead>
				<tbody>
					{% for job in jobOffers %}
						<tr>
							<td>{{ job.title }}</td>
							<td>{{ job.offerType }}</td>
							<td>{{ job.companyName }}</td>
							<td>{{ job.departmentName }}</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>

			<div class="load-more">
				<a href="{{ path('app_job_offer') }}" class="btn-link">
					<button class="btn">More Jobs</button>
				</a>

			</div>
		</section>

		<section>


			<!-- Nouvelle section: Pourquoi postuler sur NextWork -->
			<div class="reason-box full-width">
				<div class="reason-icon">
					<span class="icon">🌟</span>
				</div>
				<h3>Pourquoi postuler sur NextWork ?</h3>
				<ul>
					<li>+10 ans d'expérience en développement logiciel (backend, frontend ou fullstack).</li>
					<li>Solide maîtrise d'un ou plusieurs langages : Java, Python, Go, Typescript/React.</li>
					<li>Expérience avec les architectures cloud (AWS, GCP, Azure) et les microservices.</li>
					<li>Bonnes connaissances en bases de données SQL et NoSQL.</li>
				</ul>
				<a href="/apply" class="btn-apply">Clique ici pour postuler</a>
			</div>
		</section>


		<!-- Footer -->
		<footer class="footer">
			<p>&copy; 2025 NextWork. Tous droits réservés.</p>
		</footer>

	</body>
</html>
