{% extends 'layout/layout_frontend/connexionDepart.html.twig' %}

{% block title %}Connexion société{% endblock %}

{% block auth_content %}

    <h2>Ravi de vous revoir 👋</h2>
    <p class="subtitle">Connectez-vous pour gérer vos offres et candidatures</p>

    {# Messages flash #}
    {% for type, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ type }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    <form method="post" action="{{ path('loginSociety') }}" class="auth-form">

        <input
            type="text"
            name="company_name"
            placeholder="Nom de la société"
            required
        >

        <div class="password-field">
            <input
                type="password"
                name="password"
                placeholder="Mot de passe"
                required
            >
            <span class="eye" onclick="togglePassword(this)">👁</span>
        </div>

        <button type="submit" class="btn-primary">
            Se connecter
        </button>

    </form>

    <p class="register">
        Vous n’avez pas encore de compte ?
        <a href="{{ path('creer_Compte_Department') }}">Créer un compte</a>
    </p>

    <a class="forgot" href="#">
        Mot de passe oublié ?
    </a>

    <script>
        function togglePassword(el) {
            const input = el.previousElementSibling;
            input.type = input.type === 'password' ? 'text' : 'password';
        }
    </script>

{% endblock %}
