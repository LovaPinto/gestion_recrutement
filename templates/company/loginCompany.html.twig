{% extends 'layout/layout_frontend/connexionDepart.html.twig' %}

{% block title %}Connexion société
{% endblock %}

{% block auth_content %}
	<h2>Ravie de te revoir !</h2>

	<form method="post" action="{{ path('loginSociety') }}">

		<input type="text" name="company_name" placeholder="Nom du société" required>

		<div class="password-field">
			<input type="password" name="password" placeholder="Password" required>
			<span class="eye">👁</span>
		</div>

		<button class="btn-primary">Se connecter</button>

	</form>

	<p class="register">
		Don’t have an account ?
		<a href="{{ path('creer_Compte_Department') }}">Sign in</a>
	</p>

	<a class="forgot" href="#">Mot de passe oublié ?</a>
{% endblock %}
