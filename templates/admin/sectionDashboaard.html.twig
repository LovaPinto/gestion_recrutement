<div class="container mt-4 d-flex justify-content-center">
  <div class="row w-100 justify-content-center g-1">
    {% for  item in allmenu  %}
            <div class="col-md-3">
                <div class="c">
                        <h6 class="h6tilte" id="sd" >{{ item.h1 }}</h6>
                        <p class="card-text" id="sd"><span class="nbrOffer">{{ item.nombre }}</span><span>{{ item.type }}</span></p>
                        <p class="text-Total" id="sd">{{ item.p }}</p>
                </div>
            </div>
    {% endfor %}
  </div>
</div>
