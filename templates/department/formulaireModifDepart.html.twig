{% extends 'layout/layout_frontend/sidebar.html.twig' %}
{% set menus = ['dashboard', 'offre','utilisateur','departement','candidat'] %}
{% block title %}Modifier Département
{% endblock %}

{% block body %}
	<h2>Modifier le département</h2>

	{{ form_start(form) }}
	{{ form_row(form.departmentName) }}

	<button class="btn btn-primary">Enregistrer</button>
	{{ form_end(form) }}

	<a href="{{ path('app_department_ajout') }}">⬅ Retour</a>
{% endblock %}
